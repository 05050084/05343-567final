clear all;
close all;

f=inline('x(5)&~(x(2)*x(4)*x(6)*x(8))');
%表示先算好存表格
%5代表中心值 2 4 6 8 表示其上下左右
lut=makelut(f,3); %3*3 
c=imread('circles.tif');
cw=applylut(c,lut); %查詢表格
imshow(c);
figure,imshow(cw);
